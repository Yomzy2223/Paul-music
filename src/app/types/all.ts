export interface SongType {
  artiste?: string;
  category?: string;
  date: string;
  genre?: string;
  link: string;
  title: string;
  hostNames?: string;
}

export interface TourType {
  name: string;
  link: string;
  description: string;
  date: string;
}
